import { ChangeDetectorRef } from '@angular/core';
export declare class OnLoadComponent {
    private cd;
    isLoading: boolean;
    errorImage: string;
    defaultImage: string;
    image: string;
    constructor(cd: ChangeDetectorRef);
    onLoadImage(success: any): void;
}
